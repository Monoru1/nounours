type Props = { eyebrow: string; title: React.ReactNode; tone: 'linen' | 'night' | 'forest'; visual: 'craft' | 'memory' | 'scenes' };
const details = { craft: ['Coton brossé', 'Couture au point invisible', 'Rembourrage nuage'], memory: ['Premier secret', 'Long voyage', 'Même présence'], scenes: ['Une chambre qui s’éveille', 'Un livre oublié', 'Une lumière qui attend'] };
export function Chapter({ eyebrow, title, tone, visual }: Props) { return <section className={`chapter ${tone}`}><div className="chapter-copy"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p className="chapter-text">{visual === 'craft' ? 'L’Inspecteur examine chaque couture. Un souvenir mérite plus qu’un regard distrait.' : visual === 'memory' ? 'Il connaît les nuits trop longues, les départs et le chemin du retour.' : 'Il veille sur ce que les années n’arrivent jamais à effacer.'}</p></div><div className={`chapter-visual ${visual}`} aria-hidden="true"><div className="paper-card">{details[visual].map((detail, index) => <span key={detail} style={{'--i': index} as React.CSSProperties}>{detail}</span>)}</div><i/><i/><i/></div></section>; }
